name: Integration Branch Smoke

on:
  push:
    branches:
      - v053-upstream050-integration
    paths:
      - tools/integration-smoke.trigger

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Smoke
        run: echo integration-branch-ok
